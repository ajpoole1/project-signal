# Normalize line endings to LF on commit for all text files.
# Prevents CRLF/LF churn when editing on Windows (WSL2 + Docker remounts).
* text=auto eol=lf

# Explicitly binary — never touch line endings
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.db  binary
